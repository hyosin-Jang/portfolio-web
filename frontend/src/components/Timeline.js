import styled from "styled-components"

// TODO: 그래프에 chart.js 사용하기
const Timeline = () => {
	return <Wrapper>Chart.js..</Wrapper>
}
export default Timeline

const Wrapper = styled.div`
	display: flex;
`
